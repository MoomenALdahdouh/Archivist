Place a 7-Zip `7zz` build here after `Scripts/build-helpers.sh`.

7-Zip is GNU LGPL 2.1 (with additional BSD files and an unRAR restriction on RAR decoder sources). Archivist invokes this helper as a separate process so the application is not an LGPL derivative. Users may replace this binary.
